SSH files in this repository are templates only.

Rules:
- Never commit private keys, certificates, or known_hosts.
- Keep real SSH credentials only in local ~/.ssh.
- Track sanitized templates only:
  - config.example
